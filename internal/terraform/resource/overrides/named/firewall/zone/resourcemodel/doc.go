// Package resourcemodel keeps manual firewall zone overrides in a location the
// generator will not delete. Files here are copied verbatim into
// internal/terraform/resource/autogen so production builds always include the
// overrides.
package resourcemodel
