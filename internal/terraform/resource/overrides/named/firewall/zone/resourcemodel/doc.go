// Package resourcemodel keeps manual firewall zone overrides in a location the
// generator will not delete. Files here carry build tags so they are copied
// into internal/terraform/resource/autogen by the Makefile before builds.
package resourcemodel
